https://localhost:8080 {
    encode zstd gzip
    root * /usr/share/caddy/
    file_server
    route /node/* {
        reverse_proxy {
            to node:8443
            flush_interval -1
            transport http {
                tls
                tls_insecure_skip_verify
            }
        }
        
    }
}
